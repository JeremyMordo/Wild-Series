{% extends 'base.html.twig' %}

{% block title %}{{ 'season-home'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'season-home'|trans }}</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>{{ 'number'|trans }}</th>
                <th>{{ 'year'|trans }}</th>
                <th>{{ 'summary'|trans }}</th>
                <th>{{ 'actions'|trans }}</th>
            </tr>
        </thead>
        <tbody>
        {% for season in seasons %}
            <tr>
                <td>{{ season.id }}</td>
                <td>{{ season.number }}</td>
                <td>{{ season.year }}</td>
                <td>{{ season.description }}</td>
                <td>
                    <a href="{{ path('season_show', {'id': season.id}) }}">{{ 'show'|trans }}</a>
                    <a href="{{ path('season_edit', {'id': season.id}) }}">{{ 'update'|trans }}</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('season_new') }}">{{ 'create-season'|trans }}</a>
{% endblock %}
