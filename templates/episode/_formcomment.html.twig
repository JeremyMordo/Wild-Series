{{ form_start(form) }}
    {{ form_row(form.comment, {'attr' : {'placeholder':"Votre avis"}})}}
    {{ form_row(form.rate)}}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
