{% extends 'base.html.twig' %}

{% block title %}{{ 'edit-actor'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'edit-actor'|trans }}</h1>

    {{ include('actor/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('actor_index') }}">{{ 'back-to-list'|trans }}</a>

    {{ include('actor/_delete_form.html.twig') }}
{% endblock %}
